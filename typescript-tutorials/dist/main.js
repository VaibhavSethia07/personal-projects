var a = "1";
var hello = "world";
hello = "Thank you";
var foo = "bar";
// Specifying the datatype provides safety to developer's code. See the types while hovering over function
var getFullName = function (name, surname) {
    return name + " " + surname;
};
console.log(getFullName("Monster", "Lesson"));
